{{- if .Values.adminGui.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.adminGui.name }}-hpa
  namespace: {{ include "admin-app.namespace" . }}
  labels:
    {{- include "admin-app.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.adminGui.name }}
  minReplicas: {{ .Values.adminGui.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.adminGui.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.adminGui.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.adminGui.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.adminGui.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.adminGui.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
